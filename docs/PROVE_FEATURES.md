# Feature prove tests (browser)

**Rule:** Every high-level / big feature needs a **live prove** before calling it Done — not only unit/Vitest smokes.

**How:** MCP **Chrome DevTools** (`user-chrome-devtools`): `navigate` → poke UI via `evaluate` (click buttons, fill fields) → assert DOM / visible data → `screenshot` for evidence. Prefer the local SPA (`http://localhost:4200`) against the live API.

Unit tests prove logic. Prove tests prove **the workflow the operator sees**.

---

## Prove recipe (every big feature)

1. SPA running (`cd frontend && npm start`).
2. Sign in as demo (or Kelly) operator — real Cognito session in the browser.
3. For each control on the happy path: **click / type / select** — do not only assert that a component constructed.
4. Assert **visible outcome**: status text, table rows, chart/KPI values, dialog content, success/error messages.
5. Capture a **screenshot** at the end (or on fail).
6. Record result in the matrix below (`pass` / `fail` / `blocked` + one-line note + date).

**Blocked** is OK when backend not deployed or no fixture data — do not mark pass from unit tests alone.

---

## Feature matrix

| Feature                             | Route / entry                          | Must poke                                                 | Must see change                                                                                                              | Prove?                                                                                                                                                                                                                                                                                                     |
| ----------------------------------- | -------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sign-in                             | `/login`                               | email, password, Sign in                                  | lands authenticated shell                                                                                                    | **pass** 2026-08-08 — localhost:4200 · logo+lede+email/password+Sign in wired; Cognito `demo.operator` → `/dashboard` (Demo · Town of Wiley); bad password → “Email or password is incorrect.”                                                                                                             |
| Dashboard DataViz                   | `/dashboard`                           | sign-in refresh                                           | usage line (+ band or calm thin hint); balance **bars**; Confidence doughnut; health doughnut when meters &gt; 0; alert feed | **pass** 2026-08-05 — 24mo Town of Wiley: seasonal billed curve; Solid/319; health + alert feed ([`evidence/016`](../evidence/016-town-wiley-24mo-prove/))                                                                                                                                                 |
| Dashboard                           | `/dashboard`                           | Refresh                                                   | Confidence, balance KPI/chart, alert feed Watch/Actionable                                                                   | **pass** 2026-08-05 — 319 meters · 7 alerts · balance 42% · Solid 24 mo 54% ([`evidence/016`](../evidence/016-town-wiley-24mo-prove/))                                                                                                                                                                     |
| Upload customer                     | `/upload`                              | choose Excel/CSV, map cols, ingest                        | Excel-first copy; Mapped/Not used; import/skip summary                                                                       | **pass** 2026-08-05 — UI Excel-first steps; API dryRun 3/3 rows + 24mo header mapping ([`evidence/016`](../evidence/016-town-wiley-24mo-prove/)); file picker still blocked in automation                                                                                                                  |
| Sources                             | `/sources`                             | add/edit source, ingest source reads, map pin / fine-tune | list updates; dashboard In/Out moves; map pin persists                                                                       | **partial** 2026-08-06 — map coords + Leaflet UX shipped (SPA + sources Lambda); browser prove of pin/geocode still pending                                                                                                                                                                                |
| Alerts list                         | `/alerts`                              | Refresh, Explain                                          | rows + plainLanguage / confidence                                                                                            | **pass** 2026-08-05 — Watch balance + Actionable high-use (M-1156/M-1033/M-1089); Explain notice                                                                                                                                                                                                           |
| Alert actions (C3)                  | `/alerts`                              | Act on alert → note → Accept / Dispatch / Resolve         | status label; resolved drops; notice                                                                                         | **pass** 2026-08-05 — Accept M-1156 + note “24mo prove…” → “Alert accepted and saved”                                                                                                                                                                                                                      |
| Meter history (C5)                  | Alerts → History                       | open History                                              | **usage sparkline** + readings + alert activity                                                                              | **pass** 2026-08-05 — History M-1156: 2 canvases, age 1y 11mo, readings from 2024-08                                                                                                                                                                                                                       |
| Meter Stats viz                     | `/meters` Stats / History              | Stats button; History usage block                         | age, latest cycle, YTD, lifetime; sparkline; YoY or calm hint                                                                | **pass** 2026-08-05 — Stats M-1012: 24 reads, YTD/lifetime KPIs, 2 canvases                                                                                                                                                                                                                                |
| Export flagged                      | `/alerts`                              | Export flagged CSV                                        | file download / success notice                                                                                               | **pass** 2026-08-05 — “Downloaded flagged meters CSV…” notice + API CSV 200                                                                                                                                                                                                                                |
| Meters CRUD                         | `/meters`                              | add / edit / delete                                       | table reflects change                                                                                                        | **pass** 2026-08-04 — add/edit browser; `DELETE /meters/PROVE-013` 200 (Remove uses native confirm); 2026-08-05 list shows 319 M-* rows                                                                                                                                                                    |
| Meter map (011)                     | `/meters` Map / Both                   | SelectButton Map; marker click                            | pins + popup; “Plotted N of M”; skip without coords                                                                          | **pass** 2026-08-05 — reseeded Wiley pins: 319/320 plotted (1099 intentionally unmapped); map center on `/me`                                                                                                                                                                                              |
| Member intake (012)                 | `/onboarding`                          | step through wizard; save                                 | fields persist; complete banner when done                                                                                    | **pass** 2026-08-04 — 6-step wizard visible; System step fields + nav ([Feature 012](../specs/012-member-onboarding-reports/spec.md))                                                                                                                                                                      |
| Reports hub (012)                   | `/reports`                             | catalog tab; Run reports tab; signed-in downloads         | 5 report processes; CSV/XLS + printable Actionable sheets + ops summary                                                      | **partial** 2026-08-06 — printable work-order sheets (`format=html`) shipped (reports Lambda + SPA); prior CSV prove 2026-08-05; live poke of Open / Print sheets still pending                                                                                                                            |
| Settings (012)                      | `/settings`                            | Light/Dark toggle; profile card                           | theme persists (`html.app-dark`); session links to Account/intake/reports                                                    | **pass** 2026-08-05 — Light clears `app-dark`; Dark restores; Display + session links (Demo · Town of Wiley)                                                                                                                                                                                               |
| Theme toggle                        | shell header                           | Light → Dark                                              | `app-dark` on `<html>`; shell uses CSS vars                                                                                  | **pass** 2026-08-05 — shell + Settings SelectButton with 24mo session                                                                                                                                                                                                                                      |
| Tenant map center                   | Admin provision + `/meters`            | Map town on create; empty map center                      | `/me` returns lat/lng; empty map uses town (not CO centroid) when profile has coords                                         | **pass** 2026-08-05 — `GET /me` Wiley mapTown + 38.1542/-102.7199 zoom 13; Map uses tenant center                                                                                                                                                                                                          |
| Colorado ops KB                     | `/assistant`                           | Ask chlorine/residual after docs dropped                  | Cites `colorado-ops/*` + live CDPHE URLs; refuses inventing dosing without docs                                              | **pass** 2026-08-04 — Compose path; corpus expanded 06–09 + metadata (Feature 014)                                                                                                                                                                                                                         |
| Cognito JWT RAG                     | `/assistant` (Cognito SPA)             | residual + Watch questions signed-in                      | cites CDPHE URL from KB/local corpus; sources UI; no client tenant override                                                  | **pass** 2026-08-05 — SPA residual cites CDPHE + `s3://water-saver-dev-knowledge-…/colorado-ops/03-disinfectant-residual.md`; API `retrievalMode: bedrock-kb` + `list_alerts` ([`evidence/016`](../evidence/016-town-wiley-24mo-prove/))                                                                   |
| Account MFA                         | `/account`                             | password + MFA setup fields                               | status On / challenge on next login                                                                                          | **deferred** (accepted) 2026-08-06 — Account UI + Vitest shipped; live MFA enrollment remains Pilot ops, not Kelly/Assessment ship gate                                                                                                                                                                    |
| Assistant                           | `/assistant`                           | type prompt, send, onboarding                             | customer intro + what-you-can-do; friendly place/name greeting; no Bedrock/Mem0/Compose jargon                               | **pass** 2026-08-05 — “Hi Demo — Town of Wiley”; residual send + sources chips; no Compose/Bedrock jargon in UI ([`evidence/016`](../evidence/016-town-wiley-24mo-prove/))                                                                                                                                 |
| Assistant history hygiene (Phase A) | `/assistant`                           | load history; send hi + one real question                 | no `(town-wiley only)` / role-echo spam; welcome reinject when empty; place-named replies only                               | **pass** 2026-08-05 — Cognito `demo.operator`; empty CONV# → welcome “Wiley, CO”; hi → day-to-day ops reply; Confidence → Solid ~24 mo / ~54% coverage; zero sterile/role-echo/raw-slug hits (SPA localhost:4200)                                                                                          |
| Ask Assistant deep-link (Phase B)   | `/alerts` → `/assistant`               | Ask Assistant on a row; unknown alertId refuse            | grounded reply cites alertId / meter; unknown id says not in data; no confirmed leak from Thin Watch                         | **pass** 2026-08-05 — Cognito Demo/Wiley; Ask Assistant on stuck-1045 → grounded Actionable stuck reply (210 Vacant Lot Rd); `alertId: fake-does-not-exist-999` → “I don't see that in Town of Wiley's data… will not invent”; agent Lambda redeployed                                                     |
| Dual-path assistant docs (Phase C)  | docs + Compose tools                   | Compose stub labeled SAMPLE; product = Lambda             | SPA `composeDemo: false` never depends on Flask FAISS tools                                                                  | **pass** 2026-08-05 — `agent_tools.py` SAMPLE labels + `function-tree` / `DEMO_KNOWN_GAPS` dual-path note                                                                                                                                                                                                  |
| Upload mapper co-pilot (Phase D)    | `/upload` → `/assistant`               | Ask Assistant to map after headers load                   | reply suggests map for real headers; confirm-on-Upload copy                                                                  | **pass** 2026-08-05 — deepen handoff `headers: acct, addr, read, date, serial` → map acct→account_id … serial→meter_serial_number + Confirm before import (SPA localhost:4200)                                                                                                                             |
| LangSmith Assessment-only (Phase E) | Compose `/api/agent*`                  | traced triage + tool turn                                 | Water_Saver project URLs; Cognito path does not require LangSmith                                                            | **pass** 2026-08-03 evidence + 2026-08-05 README clarifies Cognito vs Compose                                                                                                                                                                                                                              |
| Gated write tools (Phase F)         | `/assistant`                           | CONFIRM DELETE / CHANGE                                   | Spec §0 vNext — no auto-delete                                                                                               | **deferred** — confirm gates already in `agent-context`; mutating tools not started until Spec §0 promotes                                                                                                                                                                                                 |
| Member intake live (012 wiring)     | `/onboarding` + Dashboard/Upload/Agent | signed-out preview; incomplete nudge; Path A–D downstream | public sees 6-step outline; signed-in banner + path on Confidence; Upload CIS/hints; agent uses intake                       | **pass** 2026-08-05 — signed-out 6-step outline; dashboard “Finish member intake (step 2 of 6)”; Upload Path A hint; validation blocks empty system name; Assistant “Continue intake” cites step 2 of 6 + checklist; agent Lambda redeployed                                                               |
| Kelly Review                        | `/review`                              | Start, rate steps, comments, Submit                       | panel advances; submit confirmation                                                                                          | **pass** 2026-08-04 — `kelly.review` us-east-1; rate Save&next → Skip → Submit; “review is submitted” ([evidence/013](../evidence/013-kelly-ship-prove/f1-kelly-review-submit-2026-08-04.png)); **invite sent 2026-08-06** ([KELLY_INVITE.md](KELLY_INVITE.md)); CloudFront `/review` re-verified HTTP 200 |
| Admin / CRWA                        | `/admin`                               | invite / billing fields (admin only)                      | tenant list / billing view updates                                                                                           | **pass** 2026-08-04 — Kelly `crwa_admin` roles after JWT parse fix; Admin provision UI + “1 municipality” ([f1-admin-kelly](../evidence/013-kelly-ship-prove/f1-admin-kelly-2026-08-04.png))                                                                                                               |

---

## Chrome DevTools helper patterns

```js
// Click by button label
[...document.querySelectorAll("button")]
  .find((b) => b.textContent.includes("Act on alert"))
  ?.click();

// Fill textarea / input
const ta = document.querySelector("textarea");
if (ta) {
  ta.value = "Checked register — no leak";
  ta.dispatchEvent(new Event("input", { bubbles: true }));
}

// Read visible status / notices
[...document.querySelectorAll(".p-message, td, .notice")]
  .map((el) => el.textContent.trim())
  .filter(Boolean);
```

Screenshots: call MCP `screenshot` after the critical assertion.

---

## Relationship to other checklists

| Doc                                                | Role                                                         |
| -------------------------------------------------- | ------------------------------------------------------------ |
| Vitest / backend `npm test`                        | Logic + wiring smokes                                        |
| [SMOKE_CHECKLIST.md](SMOKE_CHECKLIST.md)           | Spec §11a Kelly gate boxes                                   |
| **This file**                                      | Per-feature browser prove (buttons, fields, data vis)        |
| [ACCEPTANCE_CHECKLIST.md](ACCEPTANCE_CHECKLIST.md) | Ship/Kelly gates; link prove matrix when marking UI features |

Agents: do not claim a big UI feature Done until its row here is `pass` or explicitly `blocked` with reason.
