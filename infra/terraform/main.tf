# Root module wiring lives in cognito.tf + api.tf (Feature 003).
# Placeholder kept so `main.tf` remains the conventional entry for readers.
# See infra/README.md for apply / destroy / remote-state bootstrap.
